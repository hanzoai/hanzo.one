# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

CLAUDE.md

# Compiler output: `.hanzogui/` is the extractor's scratch dir and
# `src/styles/gui.css` is the theme sheet it writes there from src/gui/config.ts
# (vite.config's `outputCSS`). Both are rebuilt by every `pnpm build`.
.hanzogui
src/styles/gui.css
